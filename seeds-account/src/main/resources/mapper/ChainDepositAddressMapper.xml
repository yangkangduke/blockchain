<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seeds.account.mapper.ChainDepositAddressMapper">
    <resultMap id="BaseResultMap" type="com.seeds.account.model.ChainDepositAddress">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="create_time" jdbcType="BIGINT" property="createTime"/>
        <result column="update_time" jdbcType="BIGINT" property="updateTime"/>
        <result column="version" jdbcType="BIGINT" property="version"/>
        <result column="chain" jdbcType="TINYINT" property="chain"/>
        <result column="address" jdbcType="VARCHAR" property="address"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
    </resultMap>
    <select id="getAssignedAddressByUserId" resultMap="BaseResultMap">
        select * from t_chain_deposit_address
        where f_status = 1
          and f_chain = #{chain,jdbcType=TINYINT}
          and f_user_id = #{userId,jdbcType=BIGINT}
    </select>

    <select id="getIdleAddresses" resultMap="BaseResultMap">
        select *
        from t_chain_deposit_address
        where f_status = 1
          and f_chain = #{chain,jdbcType=TINYINT}
          and f_user_id = 0
    </select>

    <update id="assignAddress">
        update t_chain_deposit_address
        set f_user_id     = #{userId,jdbcType=BIGINT},
            f_version     = f_version + 1,
            f_update_time = ROUND(UNIX_TIMESTAMP(NOW()) * 1000)
        where f_chain     = #{chain,jdbcType=TINYINT}
          and f_address   = #{address,jdbcType=VARCHAR}
          and f_status = 1
          and f_user_id = 0
    </update>

</mapper>
