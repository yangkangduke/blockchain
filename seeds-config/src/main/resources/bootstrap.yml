server:
  port: 8080

spring:
  application:
    name: seeds-config

#  profiles:
#    active: native # 取本地配置文件
#  cloud:
#    config:
#      server:
#        native:
#          search-locations: classpath:/config # 配置文件的存储位置路径

# 取github上的地址配置
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AllenAi007/seeds-java
          username: 819628513@qq.com
          password: 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
          search-paths: config
          default-label: main
          force-pull: true

  kafka:
    appender:
      bootstrap:
        servers: 10.18.14.65:9092
        topic: applog-seeds