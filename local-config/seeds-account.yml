spring:
  datasource:
    url: jdbc:mysql://192.168.1.101:3306/seeds_account?useUnicode=true&characterEncoding=utf-8
    username: root
    password: Seeds123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

management:
  endpoints:
    web:
      base-path: /

mybatis:
  type-aliases-package*: com.seeds.account.model

feign:
  client:
    config:
      default:
        connect-timeout: 45000
        read-timeout: 45000
  wallet:
    access:
      access-key: XXXXX
      secret-key: "5bb40a4623ead547e4b2f15awallet.access.key.secret"


