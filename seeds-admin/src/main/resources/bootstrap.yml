spring:
  application:
    name: seeds-admin

  profiles:
    active: uat

  # 读取本地文件
#  cloud:
#    config:
#      fail-fast: true #是否启动快速失败功能，功能开启则优先判断config server是否正常
#      name: application,seeds-admin # 配置文件名 如果多个则逗号拼接 按优先级排序，如果key 相同 取 后者
#      profile: ${spring.profiles.active}
#      uri: http://seeds-config.app.prd.devseeds.link


      #读取github上文件
  cloud:
    config:
      label: main-dev #分支名称
      uri: http://seeds-config.app.prd.devseeds.link
      profile: dev
      name: application,seeds-admin #配置文件名称
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 2
        max-attempts: 10

